# 更新日志

## 2026-08-08

### 修改内容
- 修复“遮挡选中”无法遮挡中文正文的问题。
- 为答案区域增加选区记忆：在答案里选中文字后，即使点击工具栏按钮导致浏览器当前选区丢失，也能使用最近一次有效答案选区完成遮挡。
- 修复选中内容位于文本节点时无法识别所属题目的问题。
- 为“遮挡选中”按钮增加鼠标按下时阻止默认聚焦，减少按钮抢走正文选区的情况。

### 验证记录
- 执行 `npm run build` 验证 React + Vite 构建通过。

### 本次踩坑与修正
- 问题：浏览器选中的中文正文通常落在 Text 文本节点上，原代码使用 `range.startContainer.parentElement`，文本节点没有 `parentElement`，导致找不到对应题目。
  - 修正：新增 `elementFromNode()`，统一把文本节点转换到父元素后再查找 `[data-item-id]`。
  - 下次避免：处理 Selection / Range API 时不要假设 `startContainer`、`endContainer` 是元素节点。
- 问题：用户选中文字后点击工具栏按钮，浏览器可能把选区清空或转移焦点，导致“请先选中正文中的文字”。
  - 修正：用 `selectionchange` 记录最近一次答案区域选区，并在按钮 `onMouseDown` 阶段阻止默认聚焦。
  - 下次避免：任何依赖文本选区的工具栏操作，都要保存 selection range，不能只在 click 时临时读取。
- 问题：PowerShell 直接预览中文源码时可能显示乱码，容易误判文件内容损坏。
  - 修正：检查中文源码时优先用 Node `fs.readFileSync(..., "utf8")` 输出局部片段，避免被终端编码误导。
  - 下次避免：不要仅凭 PowerShell `Get-Content` 的中文显示判断源码是否真的乱码。

## 2026-08-08 点击色块白屏修复

### 修改内容
- 修复点击局部遮挡色块后页面白屏的问题。
- 点击色块时先同步读取答案区域 HTML，再写入 React state，避免 React 事件对象在异步更新函数中失效。
- 新增 React 错误边界，后续若局部交互抛错，不再直接整页白屏，而是显示刷新恢复提示。

### 验证记录
- 执行 `npm run build` 验证 React + Vite 构建通过。

### 本次踩坑与修正
- 问题：在 `setState` 的 updater 函数里读取 `event.currentTarget.innerHTML`，React 可能稍后才执行 updater，此时 SyntheticEvent 的 `currentTarget` 已经变成 `null`，导致运行时错误并白屏。
  - 修正：在点击事件同步阶段先保存 `const answerHtml = event.currentTarget.innerHTML`，updater 只使用这个普通字符串。
  - 下次避免：不要在异步回调、state updater、Promise 或 timer 中读取 React 事件对象；需要的值必须在事件处理函数同步阶段先拷贝出来。
- 问题：追加更新日志时，使用 Node stdin 动态匹配中文文件名再次失败。
  - 修正：改用 `apply_patch` 明确更新 `更新日志.md`。
  - 下次避免：涉及中文文件名写入时，优先使用明确路径补丁；不要依赖经终端 stdin 传递后的中文字符串匹配。

## 2026-08-08 同步新增章节内容

### 修改内容
- 将当前目录下新增的章节 Markdown 同步进网页永久数据文件 `public/saved-content.json`。
- 本次同步包含：
  - 第一章：艺术的本质和特征，2 个小节，20 个条目。
  - 第二章：艺术的起源，1 个小节，13 个条目。
  - 第三章：艺术的功能与艺术教育，2 个小节，19 个条目。
- 重新构建 `dist`，让构建产物也带上最新章节内容。

### 验证记录
- 执行章节解析脚本，确认共载入 3 个章节、5 个小节、52 个条目。
- 执行 `npm run build` 验证 React + Vite 构建通过。

### 本次踩坑与修正
- 问题：Node stdin 中直接写中文匹配条件时，部分终端环境可能让中文字符串匹配失败，容易把 `更新日志.md` 也误算进章节。
  - 修正：用文件名首字符 Unicode 码点 `31532` 识别“第...章”章节文件，并用 Unicode escape 表达中文数字和默认文案。
  - 下次避免：批量处理中文文件名时，关键过滤条件优先使用 Unicode 码点或明确文件列表，减少终端编码影响。

## 2026-08-08 整理独立源码目录

### 修改内容
- 创建独立项目目录 `MyRecitationWebpage`，用于集中管理背诵网页源码。
- 准备复制 React/Vite 源码、章节 Markdown、永久保存数据和项目配置，排除 `node_modules`、旧静态 HTML、PDF、构建产物等不适合提交仓库的文件。

### 验证记录
- 复制完成后已在新目录执行 `npm install` 和 `npm run build`，验证通过。
- 新目录构建成功后产生的 `node_modules/` 和 `dist/` 已通过 `.gitignore` 排除，不需要上传 GitHub。

### 本次踩坑与修正
- 问题：项目根目录里混有源码、PDF 资料、旧静态 HTML、构建产物和依赖目录，直接整体上传 GitHub 会很乱且体积大。
  - 修正：单独建立项目目录，只放部署和二次开发需要的文件。
  - 下次避免：整理可部署项目时，先区分“源码/内容源文件”和“依赖/构建产物/参考资料”，不要把整个工作目录原样提交。

## 2026-08-10 接入 Supabase 云端保存与授权编辑

### 修改内容
- 新增 Supabase 客户端配置 `src/supabaseClient.js`。
- 支持通过 `VITE_SUPABASE_URL` 和 `VITE_SUPABASE_ANON_KEY` 读取云端配置。
- 页面启动时优先读取 Supabase `recitation_content` 表中的云端内容。
- 新增邮箱密码登录、退出登录、授权邮箱检查。
- 普通访问者进入只读模式，只能背诵和导出备份，不能新增、删除、排序、编辑或导入。
- 授权邮箱登录后可以编辑内容，并点击“云端保存”永久保存到 Supabase。
- 新增 `.env.example` 和 GitHub Pages Actions 部署 workflow。
- Vite 构建时根据 GitHub 仓库名自动设置 `base`，适配 GitHub Pages 项目页路径。

### 验证记录
- 执行 `npm install @supabase/supabase-js`。
- 执行 `npm run build` 验证构建通过。
- 构建时出现 chunk size warning，原因是 Supabase 客户端包体较大，不影响功能和部署。

### 本次踩坑与修正
- 问题：Supabase RLS 只配置了 update policy 时，如果前端使用 `upsert`，可能需要 insert 权限。
  - 修正：云端保存改为 `update(...).eq("id", "main").select("id").single()`，只更新已存在的 `main` 记录。
  - 下次避免：前端保存方式要和 RLS policy 对齐；只有 update policy 时不要使用 upsert。
- 问题：GitHub Pages 项目页通常部署在 `/仓库名/` 子路径，Vite 默认 `/assets/...` 可能导致资源 404。
  - 修正：在 `vite.config.js` 中根据 `GITHUB_REPOSITORY` 自动设置 `base`。
  - 下次避免：部署到 GitHub Pages 项目页时，必须检查 Vite `base`。
- 问题：部署后静态页面不能写入 `saved-content.json`。
  - 修正：部署态使用 Supabase 数据库保存；本地未配置 Supabase 时才保留“保存到网页文件”。
  - 下次避免：不要把本地开发服务器能力当成静态托管能力。
